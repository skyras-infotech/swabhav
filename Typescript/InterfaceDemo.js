var User = {
    firstName: "Sumit",
    lastName: "Gupta",
    walk: function () { return "Person is walking"; }
};
console.log("Username is ", User.firstName + " " + User.lastName);
console.log(User.walk());
